# Архитектура пайплайна отбора

Источник порядка шагов: `_скрипты/шаги.py`. Модель отвечает только в промтах (`_промты/`); скрипты считают, сверяют цитаты и собирают таблицу. Поиск в сети разрешён только И1–И3.

```
исходник ─┬─ И1–И3 (опознание, S, авторы)
          └─ Б1–Б3 (текст, структура, чанки)
                ├─ П1 слепой разбор чанка
                └─ П1т темы чанка
                      └─ П2 карта документа (жанр)
                            └─ П3 исключения
                                  └─ П2т сюжеты темы
                                        └─ П4 строгая проверка
                                              └─ П5 D и обоснование
                                                    └─ О4 формула → У1 уровни → реестр → выгрузка
```

П1 и П1т идут по одному чанку. Объём темы берётся только из П1т, жанр документа — только из П2.

## Что оцениваем

Не «хороший ли PDF», а **материал × тема канона**.

Формула итога: `A + B + S + D` (клип 0…10). Принимается строка с итогом **не ниже 7**. Пограничный зазор: порог минус 1.

| Балл | Кто ставит | Что измеряет |
|---|---|---|
| A 1–5 | скрипт `o1_баллы.py` | объём темы: самый длинный непрерывный кусок фрагментов П1т |
| B 1–3 | скрипт `доказательность.py` | цифры П1, привязанные к теме по чанку |
| S −2…+2 | справочник / И2 | ответственность издателя, не качество статьи |
| D −2…+1 | промт П5 | перенос на финансовую отрасль |

Дескриптивный жанр документа, исключение П3, A ≤ 3 или ложное совпадение П4 формулу не считают: итог 0, измеренный A в таблице остаётся. Нет строки справочника (`S` пустой) — «в работе», не ноль.

### Шкала A

По самому длинному куску фрагментов темы (знаки, не блоки):

- ≥ 30% документа **или** ≥ 15 000 знаков → 5
- ≥ 15% / 7 000 → 4
- ≥ 5% / 3 500 → 3
- иначе 2 при полноценных блоках, иначе 1

В кандидаты на П2т / П4 / П5 идут только темы с A ≥ 4, не закрытые исключением и не закрытые дескриптивом.

### Шкала B

Категорию цифры ставит П1. «Есть ли что применить» в B не входит.

- 3: с источником больше, чем без, и внешних больше внутренних
- 2: с источником больше, внутренние не меньше внешних
- 1: без источника не меньше, чем с, либо цифр нет

### Шкала S

- +2 процедура (рецензия, стандарт, регулятор)
- +1 редакция
- 0 именной автор без институции
- −1 отвечать некому
- −2 дезинформация

ISSN бьёт домен при сопоставлении. П5 S не ставит.

### Шкала D

Целевые отрасли: финансы, инвестбанкинг, финтех, страхование, платежи.

- +1 прямо об этом
- 0 переносится без усилий
- −1 связь домысливается
- −2 механизма нет

### Гейты О4

| Условие | Итог | Что видно |
|---|---|---|
| свойство текста / ложное совпадение П4 | 0 | A измерен, формула не считается |
| нет темы | 0 | вне периметра |
| A ≤ 2 | A | вне периметра, П4 не зовётся |
| A = 3 | 3 | гейт B не открывается |
| S пустой | в работе | ждём строку справочника |
| полный расчёт, порог 7 | принято / погранично / отказ | погранично = порог минус 1 |

## Разведение ролей

**Актор** — кому метод (роль практики карточки). **Голос** — кто в тексте показал метод (авторы, we, кейс). Живут в блоках П1т и сюжетах П2т. В П4 сверяется актор сюжетов с актором карточки, не подлежащее цитаты. Голос в П4 не передаётся.

**Жанр П2** — свойство документа: можно ли воспроизвести способ на другом материале. Дескриптив закрывает все темы. **Тема П1т/П4** — совпадает ли способ с карточкой канона. П2 не смотрит на актора карточки.

## Шаги: вход и выход

Условный выход вправе отсутствовать после успеха (заявка И2). Пустой вход модельного шага — шаг пропускается.

### И. Источник

| Шаг | Вид | Имя | Что делает | Вход | Выход |
|---|---|---|---|---|---|
| И1 | скрипт | `о_источник.py вход` | Снимает первые страницы оригинала, метаданные и кандидатов DOI/ISSN/ISBN. Модель не вызывается. | исходник PDF/DOCX | `00_опознание_вход.json` |
| И1 | промт medium | `И1_опознание.md` | Опознать документ: название, авторы, регалии, издатель, тип, язык, DOI/ISSN, домен. Поиск разрешён. Содержание не оценивает. | `00_опознание_вход.json` и оригинал | `00_опознание.json` |
| И1 | скрипт | `о_источник.py свести` | Сопоставляет издателя со справочником (ISSN важнее домена). Если строки нет — заявка на И2. | `00_опознание.json`, `канон:источники.xlsx` | `00_источник.json`; условно `00_справочник_вход.json` |
| И2 | промт medium | `И2_строка_справочника.md` | Классифицирует издателя, не текст. Ставит S_база. Только если издателя нет в справочнике. Поиск разрешён. | `00_справочник_вход.json` | `00_справочник_строка.json` |
| И2 | скрипт | `о_источник.py записать` | Дописывает строку в `источники.xlsx`. Один писатель справочника. | строка, `00_источник.json`, справочник | `00_справочник_записана.json` + канон |
| И3 | скрипт | `авторы.py вход` | Сравнивает авторов со справочником. Файл пишется всегда: пустой, если все уже опознаны. | `00_источник.json`, `канон:авторы.xlsx` | `00_авторы_вход.jsonl` |
| И3 | промт medium | `И3_строка_автора.md` | Найти адрес автора. Строка заводится только с адресом. На балл материала не влияет. По автору за вызов. Поиск разрешён. | `00_авторы_вход.jsonl` (строка) | `00_авторы.jsonl` |
| И3 | скрипт | `авторы.py записать` | Пишет авторов в `авторы.xlsx`, если статус подтверждён и есть адрес. | `00_авторы.jsonl`, источник, справочник | `00_авторы_записаны.json` + канон |

### Б. Разбор

| Шаг | Вид | Имя | Что делает | Вход | Выход |
|---|---|---|---|---|---|
| Б1 | скрипт | `b1_нормализация.py` | Эталон текста: слой PDF и таблицы, NFC, снятие колонтитулов, OCR только пустых страниц. Суждений нет. Единственный индекс для цитат. | исходник | `00_слой.jsonl`, `00_текст.txt`, `00_мета.json` |
| Б2 | скрипт | `b2_структура.py` | Дерево разделов по закладкам и заголовкам. Помечает служебное. Не решает, о чём раздел. | `00_текст.txt` | `01_структура.json` |
| Б3 | скрипт | `b3_чанкинг.py` | Нарезка 2–3 страниц (~9 тыс. знаков) без перекрытия. Внутри чанка отрезки ~1000 знаков — адреса для П1т. `контекст_до` — хвост прошлого чанка, извлекать из него нельзя. | текст, слой, структура | `02_чанки.jsonl` |
| Б4 | промт medium | `П1_выжимка_чанка.md` | Слепой разбор чанка без канона тем: тезисы, кейсы, термины, цифры, адресация, продукт автора, инструменты и опоры. При смене канона не пересчитывается. По чанку за вызов. | `02_чанки.jsonl` (строка) | `03_выжимки.jsonl` |
| Б4 | скрипт | `b4_проверка_выжимок.py` | Дословность цитат П1 против `00_текст.txt`. Код 4 — править ответ, не промт. | `00_текст.txt`, `03_выжимки.jsonl` | `03_проверка.json` |
| Б4т | промт medium | `П1т_темы_чанка.md` | Единственное место, где тема привязывается к тексту. Фрагменты — где обсуждается предмет канона. Блоки — способ действия: актор, голос, бенефициар, механизм, полнота. По чанку за вызов. | чанки + канон topics (определение, направления) | `03_темы.jsonl` |
| Б4т | скрипт | `b4т_проверка_тем.py` | Цитата блока лежит в тексте и стартует внутри названных отрезков ±400 знаков. `отрезки` — пара `[от, до]`. | текст, чанки, темы, канон | `03_проверка_тем.json` |
| Б5 | скрипт | `b5_агрегат.py` | Считает объёмы тем по фрагментам (не по блокам), самый длинный кусок, привязывает инструменты П1 к темам по месту цитаты. Суждений нет. | текст, мета, структура, чанки, выжимки, темы, канон | `04_агрегат.json`, `04_агрегат_вход.json` |
| Б5 | промт high | `П2_карта_материала.md` | Документ целиком: о чём; жанр; зависимость от продукта автора; отрасль и понятия; адресат; ограничения. Сюжетов и актора карточки нет. Цитаты только из агрегата. | `04_агрегат_вход.json` | `04_решение.json` |
| Б5 | скрипт | `b5_карта.py` | Склеивает измерения, суждения П2 и оценку источника (S). Сюжетов ещё нет. | источник, агрегат, решение П2, `источники.xlsx` | `04_карта.json`, `04_саммари_сырьё.json` |

### О. Отбор

| Шаг | Вид | Имя | Что делает | Вход | Выход |
|---|---|---|---|---|---|
| О1 | промт high | `П3_исключения_тем.md` | По каждой теме канона: закрывает ли материал её исключением. Тема в тексте не ищется. Сомнение — исключение сработало и пограничное. Ответ по всем темам канона. | `04_карта.json` + канон (исключения) | `10_исключения.json` |
| О1 | скрипт | `o1_баллы.py` | Предварительный A. Дескриптив закрывает все темы. Кандидаты: A ≥ 4, не закрытые исключением. | карта, агрегат, исключения, чанки, канон | `10_кандидаты.json`, `10_сюжеты_вход.jsonl` |
| О1т | промт high | `П2т_сюжеты_темы.md` | Блоки одной темы-кандидата в сквозные сюжеты. Актор — кому метод; голос — кто показал. Каждый блок ровно в одном сюжете. По теме за вызов. | `10_сюжеты_вход.jsonl` (строка) | `10_сюжеты.jsonl` |
| О1т | скрипт | `o1_сюжеты.py` | Сводит сюжеты и пишет вход П4/П5. Без кандидатов файлы пустые, но пишутся. | карта, агрегат, кандидаты, сюжеты (необязательно), канон | `10_сюжеты_сведены.json`, `10_кандидаты_вход.jsonl` |
| О2 | промт xhigh | `П4_строгая_проверка.md` | Та ли это тема. Три сверки с карточкой: актор, бенефициар, механизм. Провал любой — ложное совпадение, формула = 0. По кандидату за вызов. | `10_кандидаты_вход.jsonl` | `11_проверка.json` |
| О3 | промт high | `П5_скоринг.md` | Ставит только D и пишет обоснование. A, B, S и вердикт не трогает. По кандидату за вызов. | `10_кандидаты_вход.jsonl`, `11_проверка.json` | `12_скоринг.json` |
| О4 | скрипт | `o4_скоринг.py` | Гейты и формула, порог 7, вердикт. Сверяет цитаты П4/П5 с картой и сюжетами. | карта, кандидаты, сюжеты, 11 и 12 (необязательно) | `отбор.md`, `13_итог.json` |

### У / Р / В

| Шаг | Вид | Имя | Что делает | Вход | Выход |
|---|---|---|---|---|---|
| У1 | скрипт | `о_уровень.py вход` | Заготовка по принятой теме и столбец канона уровней. Без принятой темы шаг пустой. | карта, агрегат, сюжеты, итог, `уровни_темы.xlsx` | `00_уровень_вход.jsonl` |
| У1 | промт high | `У1_уровень_заготовки.md` | По каждому уровню 1–4 независимо: есть ли в заготовке материал под ячейку канона, с цитатой. | `00_уровень_вход.jsonl` (строка) | `00_уровень.jsonl` |
| У1 | скрипт | `о_уровень.py свести` | Склеивает ответы У1 с заготовкой. | уровень, вход, агрегат, карта, сюжеты, итог | `00_уровень_сведён.json` |
| Р | скрипт | `реестр.py` | Собирает `реестр.json` со всем известным, включая уровни. | карта, агрегат, сюжеты, итог, уровень (необязательно) | `реестр.json` |
| В | скрипт | `выгрузка.py` | Только у материалов с хотя бы одной принятой темой: паспорт, заготовки, решение, текст, копия оригинала в `выгрузка/<папка>/`. | реестр, итог, карта, агрегат, текст, мета, источник, отбор, авторы, исходник, канон | `выгружено.json` и файлы выгрузки |

## Правка цитат (не в шаги.py)

| Шаг | Имя | Что делает | Вход | Выход |
|---|---|---|---|---|
| Б4* | `П1_правка_цитат.md` / `п1_правка_цитат.py` | Тот же объект выжимки: заменить только бракованные цитаты дословным срезом чанка | чанк, прежний ответ, брак `03_проверка.json` | `03_выжимки.jsonl` |
| Б4т* | `П1т_правка_цитат.md` / `п1т_правка_цитат.py` | То же для тем: цитата внутри отрезков блока | чанк, темы, брак `03_проверка_тем.json` | `03_темы.jsonl` |
| О2* | `П4_правка_цитат.md` / `п4_правка_цитат.py` | Цитаты П4 — подстроки карты и сведённых сюжетов после нормализации | кандидат, `11_проверка.json`, брак О4 | `11_проверка.json` |
| У1* | `у1_правка_цитат.py` | Цитаты уровней — из заготовки, не из исходника | вход У1, ответ, брак | `00_уровень.jsonl` |

## Оркестратор и канон

| Скрипт | Роль |
|---|---|
| `прогон.py` | Исполняет `шаги.py`: порядок, пропуски пустых входов, отпечатки, контракты, вызов API |
| `прогон_корпус.py` | Очередь PDF из `ИСХОДНИКИ/`. `готов()` = есть `отбор.md` и `реестр.json` |
| `прогон_последовательно.py` | Один материал за раз, 1 попытка, стоп на коде 4 — править ответ и довести |
| `контракты.py` | Схема JSON каждого промта. Код 4 — ответ не по форме |
| `модель.py` | Anthropic Messages, `claude-opus-5`. Effort из шага |
| `сводная.py` | `материалы/0_сводная.xlsx`: Сводная, Заготовки, Атрибуты |
| `источники.py` | Оценка S по справочнику |
| `канон.py` | `topics_индикаторы.xlsx`: определение, направления, исключения, актор, бенефициар, механизм, ложный триггер |

Канон: `источники.xlsx`, `авторы.xlsx`, `topics_индикаторы.xlsx`, `уровни_темы.xlsx`. Внешние входы входят в отпечаток: правка канона устаревает выходы.
